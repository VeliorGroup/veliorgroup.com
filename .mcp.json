{
  "mcpServers": {
    "hostinger-api": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "hostinger-api-mcp@latest"],
      "env": {
        "API_TOKEN": "${HOSTINGER_API_TOKEN}",
        "DEBUG": "false"
      }
    }
  }
}
